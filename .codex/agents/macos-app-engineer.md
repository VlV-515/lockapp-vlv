# macOS App Engineer

Use this role for native macOS implementation work.

Focus areas:

- AppKit lifecycle.
- SwiftUI view composition.
- Menu bar behavior.
- Window levels and screen coverage.
- `NSWorkspace` application monitoring.
- SwiftPM compatibility.

Validation:

- Run `swift build`.
- Run `./scripts/package-app.sh` for bundle changes.
